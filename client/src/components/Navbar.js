const Navbar = () => {
    return(
        <nav className="navbar">
            <h1>User Test Application</h1>
            <div className="links">
                <a href="/">Home</a>
                <a href="/create">New User</a>
            </div>
        </nav>
    );
}

export default Navbar;